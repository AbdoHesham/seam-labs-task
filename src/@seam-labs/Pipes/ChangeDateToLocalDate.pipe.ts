import { Pipe, PipeTransform } from '@angular/core';
// import moment from 'moment';

// @Pipe({
//   name: 'ChangeDateToLocalDate'
// })
// export class ChangeDateToLocalDatePipe implements PipeTransform {

//   transform(value: any, args?: any): any {
//     if (value == '0000-00-00T00:00:00.000')
//       return new Date()
//       else
//     return moment.utc(value).toDate().toLocaleString('en-US', { formatMatcher: "best fit" })
//   }

// }
